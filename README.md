A simple application to create a photo collage. It currently has a few pre-set layouts. Click and drag the edges to dynamically resize the tiles.

Now, you can also zoom in and click and drag to reposition photos in the tiles.
